<?xml version="1.0" encoding="utf-8"?>
<odoo>
<template id="template_header_style_4_custom" inherit_id="template_header_style_4" name="Header Style - 4 Custom">
    <xpath expr="//div[@class='tp-account-info']" position="replace">
        <div class="tp-account-info d-flex align-items-center justify-content-end">
            <!-- Wishlist -->
            <div class="account-section pe-3 me-3 border-end">
                <div class="d-flex align-items-center">
                    <div class="flex-shrink-0">
                        <t t-nocache="The wishlist may vary and depends on the user.">
                            <t t-set="wishcount" t-value="len(request.env['product.wishlist'].current())"/>
                            <t t-set="show_wishes" t-value="website.has_ecommerce_access()"/>
                            <div t-attf-class="o_not_editable position-relative o_wsale_my_wish #{not show_wishes and 'd-none'}">
                                <a href="/shop/wishlist">
                                    <i class="dri dri-wishlist tp-light-bg rounded-circle"/>
                                    <sup t-out="wishcount" t-attf-class="my_wish_quantity o_animate_blink badge bg-primary"/>
                                </a>
                            </div>
                        </t>
                    </div>
                    <div class="flex-grow-1 ms-3 d-none d-xl-block">
                        <div class="mb-1">My Wishlist</div>
                        <a href="/shop/wishlist"><p class="h6 mb-0">View Wishlist</p></a>
                    </div>
                </div>
            </div>

            <!-- Cuenta -->
            <div class="account-section ps-3" t-if="not user_id._is_public() or request.website.viewref('portal.user_sign_in').active">
                <div class="d-flex align-items-center">
                    <div class="flex-shrink-0">
                        <a href="#" class="tp-account-info-sidebar-action">
                            <i class="dri dri-user tp-light-bg rounded-circle"/>
                        </a>
                    </div>
                    <div class="flex-grow-1 ms-3 d-none d-xl-block">
                        <div t-nocache="User may vary." style="max-width: 150px;" class="text-truncate mb-1">
                            <t t-out="not user_id._is_public() and user_id.name or 'Guest'"/>
                        </div>
                        <a class="tp-account-info-sidebar-action" href="#"><p class="h6 mb-0">My Account</p></a>
                    </div>
                </div>
            </div>
        </div>
    </xpath>
</template>
</odoo>
