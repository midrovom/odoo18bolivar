<?xml version="1.0" encoding="utf-8"?>
<odoo>
  <template id="s_gallery_snippet" inherit_id="theme_prime.snippets" name="Products Gallery - 6 Registry">
    <xpath expr="//t[@t-snippet='theme_prime.s_gallery_5']" position="after">
      <t t-snippet="theme_prime_image.s_products_gallery"
         string="Products Gallery - 6"
         group="theme_prime_gallery"
         t-thumbnail="/theme_prime_image/static/src/img/s_banner_custom/thumb_banner.png"
         t-ignore="true">
        <keywords>gallery, album, portfolio</keywords>
      </t>
    </xpath>
  </template>

  <template id="s_products_gallery" name="Products Gallery">
    <section class="s_products_gallery py-4">
      <div class="container">

        <!-- Texto + botón en la misma fila -->
        <div class="gallery-header d-flex justify-content-between align-items-center mb-4 o_editable">
          <div>
            <h3 class="fw-bold mb-1">Título de la galería</h3>
            <p class="text-muted mb-0">Descripción breve de los productos destacados.</p>
          </div>
          <div>
            <a href="#" class="btn btn-link text-decoration-none">Ver más</a>
          </div>
        </div>

        <!-- Tarjetas de productos -->
        <div class="row row-cols-2 row-cols-md-5 g-3 justify-content-center">

          <t t-set="category_id" t-value="request.params.get('category_id') and int(request.params.get('category_id')) or False"/>
          <t t-if="category_id">
            <t t-set="products" t-value="request.env['product.template'].sudo().search([('public_categ_ids','in',[category_id])], limit=10)"/>
          </t>
          <t t-else="">
            <t t-set="products" t-value="request.env['product.template'].sudo().search([], limit=10)"/>
          </t>

          <t t-foreach="products[:10]" t-as="product">
            <div class="col">
              <div class="product-card text-center p-3 h-100 border rounded shadow-sm">

                <img t-att-src="'/web/image/product.template/%s/image_1920' % product.id"
                     t-att-alt="product.name"
                     class="img-fluid mb-2"/>

                <hr class="my-2"/>

                <h5 class="fw-bold text-truncate" t-att-title="product.name">
                  <t t-esc="product.name"/>
                </h5>

                <ul class="list-unstyled small text-muted text-start text-wrap">
                  <t t-foreach="product.attribute_line_ids" t-as="attr">
                    <t t-if="attr.attribute_id.name != 'Marca'">
                      <t t-foreach="attr.value_ids" t-as="val">
                        <li class="d-flex align-items-center mb-1 text-truncate">
                          <t t-if="val.dr_image">
                            <img t-att-src="'/web/image/product.attribute.value/%s/dr_image' % val.id"
                                 t-att-alt="val.name"
                                 class="attr-icon me-2"/>
                          </t>
                          <span><t t-esc="val.name"/></span>
                        </li>
                      </t>
                    </t>
                  </t>
                </ul>

                <div class="price fw-bold mt-2 text-truncate text-primary">
                  $<t t-esc="product.list_price"/>
                </div>

              </div>
            </div>
          </t>
        </div>

      </div>
    </section>
  </template>
</odoo>
