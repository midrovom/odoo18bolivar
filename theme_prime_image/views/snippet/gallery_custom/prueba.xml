<?xml version="1.0" encoding="utf-8"?>
<odoo>
  <template id="s_gallery_snippet" inherit_id="theme_prime.snippets" name="Products Gallery - 6 Registry">
    <xpath expr="//t[@t-snippet='theme_prime.s_gallery_5']" position="after">
      <t t-snippet="theme_prime_image.s_products_gallery"
         string="Products Gallery - 6"
         group="theme_prime_gallery"
         t-thumbnail="/theme_prime_image/static/src/img/s_banner_custom/thumb_banner.png">
        <keywords>gallery, album, portfolio</keywords>
      </t>
    </xpath>
  </template>

<template id="s_products_gallery" name="Products Gallery">
  <section class="s_products_gallery o_snippet py-4"
           data-oe-model="product.public.category"
           data-oe-many2one="category_id">
    <div class="container">
      <div class="row row-cols-2 row-cols-md-5 g-3">

        <!-- Obtener categoría seleccionada -->
        <t t-set="category_id" t-value="category_id or False"/>

        <!-- Selección de productos -->
        <t t-if="category_id">
          <t t-set="products"
             t-value="request.env['product.template'].sudo().search([('public_categ_ids','in',[category_id])], limit=10)"/>
        </t>
        <t t-else="">
          <t t-set="products"
             t-value="request.env['product.template'].sudo().search([], limit=10)"/>
        </t>

        <!-- Iterar productos -->
        <t t-foreach="products" t-as="product">
          <div class="col">
            <div class="product-card text-center p-3 h-100">

              <!-- Imagen -->
              <img t-att-src="'/web/image/product.template/%s/image_1920' % product.id"
                   t-att-alt="product.name"
                   class="img-fluid mb-2"/>

              <hr class="my-2"/>

              <!-- Nombre -->
              <h5 class="fw-bold text-truncate" t-att-title="product.name">
                <t t-esc="product.name"/>
              </h5>

              <!-- Precio + Botón editable -->
              <div class="d-flex flex-wrap justify-content-between align-items-center mt-auto">
                <div class="price fw-bold mb-2 text-truncate">
                  $<t t-esc="product.list_price"/>
                </div>
                <form t-att-action="'/shop/cart/update'" method="post" class="m-0 flex-grow-1 flex-md-grow-0">
                  <input type="hidden" name="product_id" t-att-value="product.id"/>
                  <button type="submit"
                          class="btn o_we_button rounded-pill btn-sm w-100 w-md-auto mt-2 mt-md-0">
                    <span>COMPRAR</span>
                  </button>
                </form>
              </div>

            </div>
          </div>
        </t>
      </div>
    </div>
  </section>
</template>
</odoo>
